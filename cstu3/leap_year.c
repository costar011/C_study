/* 쉽게 풀어쓴 C언어 express 236 */

#include <stdio.h>

int main(void) {

int year;

printf("분자를 입력하세요: ");
scanf("%d", &n);

if( d == 0)
    printf("0으로 나눌 수 없습니다.\n");
else    
    printf("결과는 %d입니다.\n", result);

return 0;

}